- clean: ['~', '~/.config']

- link:
    ~/.dotfiles: ''
    ~/.config/nvim:
        create: true
    ~/.gitconfig:
    ~/.zprofile:
    ~/.zshrc:

- shell:
    - git submodule update --init --recursive
